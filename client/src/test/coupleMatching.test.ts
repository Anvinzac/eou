import { describe, expect, it } from 'vitest';

// Matching logic moved to server — covered by server/src/services/coupleMatching.test.ts
describe('coupleMatching (client)', () => {
  it('defers to server service tests', () => {
    expect(true).toBe(true);
  });
});
